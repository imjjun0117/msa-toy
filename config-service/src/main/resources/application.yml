server:
  port: 8888

spring:
  application:
    name: config-service
  profiles:
    active: git
  cloud:
    config:
      server:
        native:
          search-locations: file://${user.home}/hwangseongjun/mywork/git-local-repo
        git:
          uri: https://github.com/joneconsulting/spring-cloud-config
          default-label: master
#          username: [username]
#          password: [password]