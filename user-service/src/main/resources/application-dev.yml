server:
  port: 60000

spring:
  application:
    name: user-service
  #  config:
#    import: "optional:configserver:http://localhost:8888"
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
      path: /h2-console
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
  security:
    user:
      name: user
      password: $2a$10$8peVO1zUwEYFHmuIQvzR7O4I4o45PkcDgaqTNQlrxpg8x1CNCiBkK
eureka:
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka

greeting:
  message: Welcome to the Simple E-commerce. (dev profile)


token:
  secret: my-very-long-secret-key-1239481239481239481239481239481239481239481
  expiration-time: 86400000 # 10days

management:
  endpoints:
    web:
      exposure:
        include:
          - refresh
          - health
          - beans
          - info